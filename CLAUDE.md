La fuente real de este documento vive en Supabase (proyecto `cazapisos`, tabla `project_docs`, `doc_name='CLAUDE.md'`) — consúltala vía REST (`GET {SUPABASE_URL}/rest/v1/project_docs?doc_name=eq.CLAUDE.md&select=content` con la publishable key en `apikey`/`Authorization`) antes de trabajar en este repo; este archivo es solo un puntero para que el harness no cargue contexto obsoleto.
