SUPABASE_URL=
SUPABASE_KEY=
GITHUB_TOKEN=
APP_PIN=
# Secreto para firmar la cookie de sesión (HMAC), independiente de APP_PIN
# (que amigo y su mujer conocen para entrar). Genera uno largo y random,
# por ejemplo: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=
